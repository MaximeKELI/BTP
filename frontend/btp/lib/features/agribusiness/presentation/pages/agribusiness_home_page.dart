import 'package:flutter/material.dart';

class AgribusinessHomePage extends StatelessWidget {
  const AgribusinessHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Agribusiness'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text('Page Agribusiness - En construction'),
      ),
    );
  }
}
